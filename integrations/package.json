{
  "name": "@seed/integrations",
  "version": "1.0.0",
  "description": "External integration adapters: GSP (e-invoice), payment gateways, SMS/WhatsApp, Tally/QuickBooks export",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup index.ts --format cjs --out-dir dist --dts",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "dotenv": "^17.3.1",
    "nodemailer": "^7.0.13",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/nodemailer": "^7.0.10",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
