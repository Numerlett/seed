{
  "permissions": {
    "allow": [
      "Bash(pnpm add *)",
      "Bash(pnpm --filter web build)",
      "Bash(pnpm --filter @seed/database build)",
      "Bash(git mv *)",
      "Bash(rmdir jobs/src)",
      "Bash(mkdir -p pdf/templates)",
      "Bash(rmdir pdf/src/templates pdf/src)",
      "Bash(mkdir -p jobs/lib)",
      "Bash(mkdir -p integrations/adapters)",
      "Bash(npx --yes expo install expo-secure-store expo-image-picker react-native-svg)",
      "Bash(npx --yes expo install --fix)",
      "Bash(pnpm --filter @seed/schemas build)",
      "Bash(npx tsc *)",
      "Bash(npx expo *)",
      "Bash(pnpm --filter @seed/server --filter @seed/api --filter @seed/schemas lint)",
      "Bash(pnpm -r --filter \"!web\" lint)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "code-review-graph update",
            "timeout": 5000
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "code-review-graph status --json",
            "timeout": 3000
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "code-review-graph detect-changes --brief",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
