# Expo's Metro bundler + autolinking expect a flat node_modules layout.
# pnpm's default isolated store hides transitive deps (e.g. @expo/metro-runtime)
# from Metro, so we hoist for the whole workspace. This is Expo's documented
# recommendation for pnpm monorepos.
node-linker=hoisted
